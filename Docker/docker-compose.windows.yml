# Windows/WSL2 + VcXsrv override.
# Usage: docker compose -f docker-compose.yml -f docker-compose.windows.yml run --rm px4-sitl

services:
  px4-sitl:
    network_mode: host
    environment:
      - DISPLAY=host.docker.internal:0.0
    ports:
      - "14540:14540/udp"
      - "14550:14550/udp"
